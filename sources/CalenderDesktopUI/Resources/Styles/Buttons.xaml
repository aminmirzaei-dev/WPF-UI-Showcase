<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:CalendarDesktopUI.Controls"
                    xmlns:icons="http://schemas.fontawesome.io/icons/">

    <Style TargetType="{x:Type controls:MenuButtonItem}">

        <Setter Property="Background"
                Value="White" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:MenuButtonItem}">

                    <Border Background="{TemplateBinding Background}"
                            Padding="10,5"
                            BorderThickness="0"
                            Width="140">

                        <StackPanel Orientation="Horizontal">

                            <icons:ImageAwesome Icon="{Binding Icon,
                                   RelativeSource={RelativeSource TemplatedParent}}"
                                                Width="13"
                                                Height="13"
                                                Foreground="#666666"
                                                Margin="5,0,20,0" />

                            <TextBlock Text="{Binding Caption,
                                   RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="#666666"
                                       FontSize="14" />

                        </StackPanel>

                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">
                <Setter Property="Background"
                        Value="#e9e9e9" />
            </Trigger>
        </Style.Triggers>

    </Style>


    <Style x:Key="YearButton"
           TargetType="Button">
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="Foreground"
                Value="#BABABA" />
        <Setter Property="FontSize"
                Value="22" />
        <Setter Property="Margin"
                Value="12 0" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border BorderThickness="0">
                        <ContentPresenter VerticalAlignment="Center"
                                          HorizontalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">
                <Setter Property="Foreground"
                        Value="#C73F69" />
            </Trigger>
            <Trigger Property="IsMouseCaptured"
                     Value="True">
                <Setter Property="Foreground"
                        Value="#8a2b42" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MonthButton"
           TargetType="Button"
           BasedOn="{StaticResource YearButton}">
        <Setter Property="FontSize"
                Value="18" />
    </Style>

    <Style x:Key="BigButton"
           TargetType="Button"
           BasedOn="{StaticResource YearButton}">
        <Setter Property="Foreground"
                Value="Orange" />
    </Style>

</ResourceDictionary>